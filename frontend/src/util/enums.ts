export enum PromiseState {
  PENDING = 'pending',
  DONE = 'done',
  ERROR = 'error'
}

export enum SocketEvent {
  Created = 'created',
  Updated = 'updated',
  Patched = 'patched',
  Removed = 'removed'
}
